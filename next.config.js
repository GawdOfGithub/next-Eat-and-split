/** @type {import('next').NextConfig} */
/** @type {import('next').NextConfig} */
/** @type {import('next').NextConfig} */
module.exports = {
    images: {
      domains: ['i.pravatar.cc'], // Allow images from this domain
    },
  };